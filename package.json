{
  "name": "essayrate-server",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy --minify",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings"
  },
  "dependencies": {
    "@google/genai": "^0.9.0",
    "hono": "^4.7.7"
  },
  "devDependencies": {
    "wrangler": "^4.4.0"
  }
}
